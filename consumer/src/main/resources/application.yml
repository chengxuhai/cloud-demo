server:
  port: 8201

spring:
  application:
    name: consumer

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:7001/eureka

feign:
  hystrix:
    enabled: true

